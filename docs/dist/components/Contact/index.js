/* src\components\Contact\index.svelte generated by Svelte v3.31.0 */
import {
	SvelteComponent,
	add_render_callback,
	append,
	assign,
	attr,
	check_outros,
	create_bidirectional_transition,
	create_component,
	destroy_component,
	destroy_each,
	detach,
	element,
	get_spread_object,
	get_spread_update,
	group_outros,
	init,
	insert,
	mount_component,
	safe_not_equal,
	space,
	text,
	transition_in,
	transition_out
} from "../../../web_modules/svelte/internal.js";

import { fly, fade } from "../../../web_modules/svelte/transition.js";
import { quintOut } from "../../../web_modules/svelte/easing.js";
import NextButton from "../NextButton/index.js";
import { contacts } from "../../constants/index.js";

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[1] = list[i];
	child_ctx[3] = i;
	return child_ctx;
}

// (22:4) {#each contacts as item, index}
function create_each_block(ctx) {
	let a;
	let div1;
	let img;
	let img_src_value;
	let img_alt_value;
	let t0;
	let div0;
	let t1_value = /*item*/ ctx[1].title + "";
	let t1;
	let t2;
	let div2;
	let t3_value = /*item*/ ctx[1].text + "";
	let t3;
	let t4;
	let a_href_value;
	let a_transition;
	let current;

	return {
		c() {
			a = element("a");
			div1 = element("div");
			img = element("img");
			t0 = space();
			div0 = element("div");
			t1 = text(t1_value);
			t2 = space();
			div2 = element("div");
			t3 = text(t3_value);
			t4 = space();
			attr(img, "class", "mb-5");
			if (img.src !== (img_src_value = /*item*/ ctx[1].icon)) attr(img, "src", img_src_value);
			attr(img, "alt", img_alt_value = /*item*/ ctx[1].title);
			attr(div0, "class", "font-bold");
			attr(div1, "class", "flex justify-center items-center flex-col w-16");
			attr(div2, "class", "text-sm font-medium");
			attr(a, "href", a_href_value = /*item*/ ctx[1].href);
			attr(a, "target", "_blank");
			attr(a, "class", "flex justify-center items-center flex-col w-48");
		},
		m(target, anchor) {
			insert(target, a, anchor);
			append(a, div1);
			append(div1, img);
			append(div1, t0);
			append(div1, div0);
			append(div0, t1);
			append(a, t2);
			append(a, div2);
			append(div2, t3);
			append(a, t4);
			current = true;
		},
		p(new_ctx, dirty) {
			ctx = new_ctx;
		},
		i(local) {
			if (current) return;

			add_render_callback(() => {
				if (!a_transition) a_transition = create_bidirectional_transition(
					a,
					fly,
					{
						delay: /*index*/ ctx[3] * 100,
						duration: 1000,
						x: 0,
						y: 15,
						opacity: 0,
						easing: quintOut
					},
					true
				);

				a_transition.run(1);
			});

			current = true;
		},
		o(local) {
			if (!a_transition) a_transition = create_bidirectional_transition(
				a,
				fly,
				{
					delay: /*index*/ ctx[3] * 100,
					duration: 1000,
					x: 0,
					y: 15,
					opacity: 0,
					easing: quintOut
				},
				false
			);

			a_transition.run(0);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(a);
			if (detaching && a_transition) a_transition.end();
		}
	};
}

function create_fragment(ctx) {
	let div1;
	let h3;
	let h3_transition;
	let t1;
	let div0;
	let t2;
	let nextbutton;
	let current;
	let each_value = contacts;
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	const out = i => transition_out(each_blocks[i], 1, 1, () => {
		each_blocks[i] = null;
	});

	const nextbutton_spread_levels = [/*buttonProps*/ ctx[0]];
	let nextbutton_props = {};

	for (let i = 0; i < nextbutton_spread_levels.length; i += 1) {
		nextbutton_props = assign(nextbutton_props, nextbutton_spread_levels[i]);
	}

	nextbutton = new NextButton({ props: nextbutton_props });

	return {
		c() {
			div1 = element("div");
			h3 = element("h3");
			h3.textContent = "Say Hı";
			t1 = space();
			div0 = element("div");

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			t2 = space();
			create_component(nextbutton.$$.fragment);
			attr(h3, "class", "text-6xl font-black mb-20");
			attr(div0, "class", "flex justify-center items-center");
			attr(div1, "class", "text-center");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, h3);
			append(div1, t1);
			append(div1, div0);

			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(div0, null);
			}

			insert(target, t2, anchor);
			mount_component(nextbutton, target, anchor);
			current = true;
		},
		p(new_ctx, [dirty]) {
			ctx = new_ctx;

			if (dirty & /*contacts, quintOut*/ 0) {
				each_value = contacts;
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
						transition_in(each_blocks[i], 1);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						transition_in(each_blocks[i], 1);
						each_blocks[i].m(div0, null);
					}
				}

				group_outros();

				for (i = each_value.length; i < each_blocks.length; i += 1) {
					out(i);
				}

				check_outros();
			}

			const nextbutton_changes = (dirty & /*buttonProps*/ 1)
			? get_spread_update(nextbutton_spread_levels, [get_spread_object(/*buttonProps*/ ctx[0])])
			: {};

			nextbutton.$set(nextbutton_changes);
		},
		i(local) {
			if (current) return;

			add_render_callback(() => {
				if (!h3_transition) h3_transition = create_bidirectional_transition(
					h3,
					fly,
					{
						duration: 1000,
						x: 0,
						y: 15,
						opacity: 0,
						easing: quintOut
					},
					true
				);

				h3_transition.run(1);
			});

			for (let i = 0; i < each_value.length; i += 1) {
				transition_in(each_blocks[i]);
			}

			transition_in(nextbutton.$$.fragment, local);
			current = true;
		},
		o(local) {
			if (!h3_transition) h3_transition = create_bidirectional_transition(
				h3,
				fly,
				{
					duration: 1000,
					x: 0,
					y: 15,
					opacity: 0,
					easing: quintOut
				},
				false
			);

			h3_transition.run(0);
			each_blocks = each_blocks.filter(Boolean);

			for (let i = 0; i < each_blocks.length; i += 1) {
				transition_out(each_blocks[i]);
			}

			transition_out(nextbutton.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div1);
			if (detaching && h3_transition) h3_transition.end();
			destroy_each(each_blocks, detaching);
			if (detaching) detach(t2);
			destroy_component(nextbutton, detaching);
		}
	};
}

function instance($$self) {
	const buttonProps = {
		nextPageIndex: 1,
		nextPageNumber: "01",
		nextPageText: "ABOUT"
	};

	return [buttonProps];
}

class Contact extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Contact;